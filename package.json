{
  "name": "bodegacopeco",
  "version": "1.0.0",
  "description": "Sistema de administración de bodegas COPECO",
  "main": "index.php",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tailwindcss -i src/css/app.css -o src/css/style.css --watch",
    "build": "npx tailwindcss -i src/css/app.css -o src/css/style.css --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.15",
    "postcss": "^8.4.28",
    "tailwindcss": "^3.3.3"
  }
}
